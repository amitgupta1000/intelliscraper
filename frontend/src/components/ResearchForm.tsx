// ResearchForm removed — replaced by a minimal placeholder since URL-first `RequestForm` is used.
import React from 'react';

const ResearchForm: React.FC = () => null;

export default ResearchForm;
