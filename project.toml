[tool.poetry]
name = "intellisearch"
version = "0.1.0"
description = "IntelliSearch backend"
authors = ["Your Name <your@email.com>"]

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.100.0"
uvicorn = "^0.23.0"
gunicorn = "^22.0.0"
aiofiles = "^23.2.1"
# ...other dependencies...


# project.toml
[build]
# Optionally specify a buildpack builder, though 'google-22' (default) is usually fine for Python
builder = "gcr.io/buildpacks/builder:google-22"

[start]
# This explicitly tells the buildpack how to start your application
# Adjust 'main:app' if your entry point is different
command = "gunicorn main:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8080 --timeout 60"
